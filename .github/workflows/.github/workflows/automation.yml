name: My First Automation

on:
  push:
    branches: [ "main" ]

jobs:
  automation-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run automation
        run: echo "Automation successfully created by Ashish 🚀"
